# Dermalens OCR — 런타임 의존성
# 설치: pip install -r requirements.txt
# (버전은 현재 개발 환경 기준으로 고정)

# ── OCR 엔진 (Google Cloud Vision) ──────────────────────────
google-cloud-vision==3.7.2
google-auth==2.52.0

# ── 이미지 처리 ─────────────────────────────────────────────
opencv-python==4.6.0.66
numpy==1.23.5
pillow==12.2.0

# ── AI 추출 / 검증 ──────────────────────────────────────────
openai==2.38.0
anthropic==0.100.0

# ── HTTP / 환경설정 / 유틸 ──────────────────────────────────
requests==2.33.1
python-dotenv==1.2.2
tqdm==4.67.3

# ── 웹 서버 (FastAPI) ───────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
